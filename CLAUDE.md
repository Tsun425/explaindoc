# explaindoc — 生徒向けプログラミング解説スライド

中高生の生徒に配る解説スライドをHTMLで作り、GitHub Pages で公開して
Google Classroom にリンクを貼るためのリポジトリ。

**HTMLを書くのはあなた（Claude）です。** ユーザーはHTMLを編集しません。
ユーザーの役割は、スライドの題材を決めること、スクリーンショットを撮ること、
公開前にプレビューして承認することの3つです。

---

## スライドを1本作る手順

1. **`/grill-me` で題材を詰める。** 下の「毎回確認すること」を全部埋めてから書き始める。
2. **`_template/sample-lesson.html` をコピー**して、置き場所に配置する。
3. 本文を書く。デザインは触らない。
4. **ローカルのHTMLを `open` でブラウザに表示し、ユーザーの承認を待つ。**
5. 承認が出たら `index.html` と該当分野の `index.html` にリンクを追記する。
6. `git add . && git commit && git push` する。
7. 公開URLを伝える。反映まで1〜2分かかる。

**承認前に push しない。** これは例外のないルール。

### 毎回確認すること

- **対象** — その回の生徒の学年と進度。中学生と高校生では説明の深さが変わる
- **前提知識** — このスライドを読む前に何を終えているか
- **ゴール** — 読み終えたとき何ができていればよいか
- **環境のバージョン** — Python 3.x / Unity 6 / Spring Boot 3.x など。
  バージョン違いは生徒が最も詰まる原因
- **スクリーンショットの要否** — 必要ならユーザーに撮影を依頼する。**あなたには撮れない**
- **次に読むスライド** — 学習の道筋をつなぐ

---

## 置き場所と名前

```
index.html              分野一覧（トップ）
python/index.html       単元の地図 + 構文チートシート
python/list-basics.html 個別の授業スライド
python/list-basics/     このスライド専用の画像フォルダ
  01-vscode.png
cs/how-to-get-unstuck.html   詰まったときの動き方（全スライドからリンク）
```

分野は `python/` `unity/` `java/` `cs/` の4つ。
Unity の C# は `unity/` に置く。データ構造とアルゴリズムは `cs/` に置く。

- ファイル名は **英小文字とハイフン**。日本語URLは Classroom で長大な文字列になる
- 画像はスライドごとの専用フォルダに入れ、`01-` から連番を振る
- **ディレクトリは最初のスライドを書くときに作る。** 空の箱を先に作らない

---

## スライドの中身

スライド1本 = 授業1コマ = **90分**。本文3000〜5000字、コード例5〜8個、演習2〜3問が目安。
これを超えるなら2本に分ける。

構成は上から順に:

1. 分野ラベルと回数、タイトル
2. 対象・前提・所要時間のチップ、最終更新日
3. もくじ
4. 本文（見出しごとにセクション）
5. 手順ステップ
6. やってみよう（答えは `<details>` で隠す）
7. まとめ
8. `cs/how-to-get-unstuck.html` へのリンク
9. 次に読むスライド / 単元の地図へのリンク

**「よくあるエラー」の一覧は作らない。** 代わりに
`cs/how-to-get-unstuck.html`（エラーの読み方、検索の仕方、ChatGPTへの聞き方）へ誘導する。
生徒が実際に質問してきた内容は、そのスライドの注意書きに追記して育てる。

分野ごとの `index.html` は、単元の地図（学ぶ順序と各回へのリンク）と
構文チートシート（解説なしの一覧）の2部構成にする。個別スライドの本文を複製しない。

---

## 技術仕様

- **CSSとJavaScriptはHTMLに埋め込む。外部CDNは読み込まない。** 学校のネットワークが
  外部ドメインを遮断していても表示できる状態を保つため
- **画像だけは外部ファイル**にする
- **色はすべてCSS変数で定義する。** HTMLに色を直書きしない。
  将来ダークモードを足すとき `@media (prefers-color-scheme: dark)` を1ブロック
  追加するだけで済ませるため
- 全ページの `<head>` に `<meta name="robots" content="noindex, nofollow">` を入れる
- 本文は 17px / 行間 1.9。日本語は行間を広げないと読みにくい
- ページ下部に最終更新日を `YYYY-MM-DD` で表示する
- 印刷時はコピーボタン・もくじ・「次に読む」を隠し、**演習の答えは開いた状態**にする

---

## デザイン

`_template/sample-lesson.html` が唯一の見本。**新しいスライドはこれをコピーして作る。**
配色、余白、角丸、影、コードブロック、手順ステップ、注意書き、演習の折りたたみが
すべて実装済みなので、ここに無い部品を思いつきで足さない。

このファイルには Fraunces フォントが base64 で1行に埋め込まれている。
その行は読まず、触らず、そのまま残す。

### 色（Concept 06 Design System より）

| 変数 | 値 | 用途 |
|---|---|---|
| `--bg` | `#FBF7F2` | 地 |
| `--surface` | `#FFFFFF` | カード |
| `--surface-alt` | `#F3EDE7` | コードブロックの地 |
| `--border` | `#E0D6CC` | 罫線 |
| `--text` | `#2B1F16` | 本文 |
| `--text-sub` | `#6B645D` | 補助テキスト、コメント |
| `--accent-dark` | `#A34A2B` | リンク、ボタン塗り、小さい文字、予約語 |
| `--accent` | `#C75A36` | ホバー、24px以上の見出し、アイコン |
| `--success` | `#2E7D32` | 文字列リテラル |
| `--warning` | `#D97706` | 注意 |
| `--error` | `#B3261E` | エラー |
| `--info` | `#3E6B8A` | 補足、数値リテラル |

`--accent` (`#C75A36`) は小さい文字に使わない。コントラストが足りない。
文字には `--accent-dark` を使う。

余白は4pxの倍数。角丸は操作要素999px、コードブロック8px、カード16px。
状態変化は200ms、`cubic-bezier(.16,1,.3,1)`。

エラーや注意は色だけで示さず、アイコンと説明文を必ず伴わせる。
文字色は背景に対して4.5:1以上のコントラストを保つ。

### フォント

日本語はOSのシステムフォントで表示する（Windowsの游ゴシック等）。
**Fraunces は日本語のグリフを持っていない。** 日本語に指定すると書体が混在する。

Fraunces を当ててよいのは、日本語が混ざりようのない要素だけ:
分野ラベル（`Python`）、セクション番号（`01`）、日付、コードブロックの言語名、
本文中の単独の数字。これらには `.latin` クラスまたは個別指定を使う。

分野ごとにアクセント色を変えるかは未定。最初の2〜3本を作って生徒の反応を見てから決める。
それまでは全分野で同じ配色を使う。

---

## 公開

- リポジトリ: `Tsun425/explaindoc`（public）
- URL: `https://tsun425.github.io/explaindoc/`
- `main` に push すると1〜2分で反映される
- `.nojekyll` があるので、書いたHTMLがそのまま配信される

更新しても生徒のブラウザに古い版が出ることがある。
その場合は **`Ctrl + Shift + R`** で再読み込みするよう伝える（生徒はWindows）。

---

## スライドに書かないこと

生徒の氏名、成績、写真、その他の個人情報。学校固有の非公開情報。
リポジトリは public で、URLを知っていれば誰でも閲覧できる。

---

## 生徒の環境

- 端末は **Windows**
- **ChatGPTの無料枠**が使える。年齢制限と学校の方針はクリア済み
- Webの検索に制限はない
- 学んでいるのは Python、Unity（C#）、Java（Spring Boot）、
  コンピュータサイエンス、データ構造とアルゴリズム
- 1クラス10人以下
